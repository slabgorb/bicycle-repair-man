workflow:
  name: architecture
  description: Collaborative architectural decision-making (stepped).
  version: "0.4.0"
  type: stepped
  agent: architect
  steps:
    path: ./architecture/steps/
    pattern: 'step-{nn}-*.md'
  triggers:
    types: [architecture, design, adr]
    default: false
